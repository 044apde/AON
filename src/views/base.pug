doctype html
html(lang="ko")
    head  
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel="stylesheet" href="/css/base.css")

    body      
        .sideBar
            div
                a(href="/")
                    img.logo(src="/images/logo.png")
            .profileBox
                img.profileImage(src="/images/profile_icon.png" alt = "profile_icon")
                .userName #{loggedInUser.username}
                .userID (#{loggedInUser.id})
                .campus 한양대학교 에리카 캠퍼스
                .grade #{loggedInUser.grade}
                button.proflieBtn(type='button' onclick = "location.href = '/user/proflie'") 내 정보
        .main
            .nav
                .big
                    a.E(href="/") 홈
                    a.E(href ="#") 뮴팅
                    .dropdown
                        a.dropbtn 게시판
                        .dropdown-content
                            a(href='/board/자유') 자유 게시판
                            a(href='/board/정보') 정보 게시판
                            a(href='/board/장터') 장터 게시판
                            a(href='/board/장터') 취창업 게시판
            .searchBar
                div
                    img(src="/images/icon_fire.png")
                    p 인기 게시글
                    a(href="#").popularPost=`"홈페이지 서비스 아이디어 공모전 안내"`
                div
                    form(method="GET").searchBox
                        input(placeholder="검색어를 입력하세요!" type="text" name="keyword")
            //- contents 아래에 code 작성하면 됩니다
            .contents
                block content